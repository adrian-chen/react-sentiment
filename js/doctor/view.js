var DoctorView = React.createClass({

    getInitialState: function() {
        return {doctor: null}
    },

    render: function() {
        
            return (
                <h4>Enter a URL (or two) and hit <i>Go!</i> to do some sentiment analysis.</h4>
            )
    }
})